# dependencies
node_modules/
.pnp
.pnp.js

# python — local venvs (e.g. services/akshare-wrapper); recreate via requirements.txt
.venv/
venv/
__pycache__/
*.py[cod]

# testing
coverage/

# expo-env
.expo/
dist/
web-build/

# native-env
ios/
android/

# build
build/

# env — ignore all variants except committed templates (ADR 007)
.env*
!.env.example
!.env*.example

# supabase local dev (supabase start) + CLI tmp output (login / functions deploy)
.supabase/
supabase/.temp/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# turbo
.turbo/

# qoder
.qoder/

# cursor (skills mirrored from .claude/skills via tools/sync-skills.sh)
.cursor/skills/

# agents skill mirror (SkillsMP / other tools — regenerated, not canonical)
.agents/

# codex (local config — contains API keys / MCP tokens, never commit)
.codex/

# planning scratchpad (Delta screenshots, IA reference, designer drafts — local only)
.tempref/

# Repomix context bundles (local LLM snapshots — never commit)
.specify/codectx/
repomix-output.xml
repomix-output.md
repomix-output.json
repomix-output.txt





# system
.DS_Store
Thumbs.db

# MCP config (contains personal tokens — each dev uses their own)
.mcp.json

# Spike sandbox build artifacts
tools/spike-heroui-native/node_modules/
tools/spike-heroui-native/dist/
tools/spike-heroui-native/.cache/
tools/spike-heroui-native/.expo/
tools/spike-heroui-native/uniwind-types.d.ts
tools/spike-heroui-native/spike-render.png
tools/spike-heroui-native/pnpm-lock.yaml

# Claude Code per-developer permissions (contains shell command history)
.claude/settings.local.json

/**
 * ChartSkeleton — chart loading placeholder (draw-line animation).
 *
 * @deprecated Prefer importing `ChartDrawLoading` directly. Kept for existing exports.
 */

export {
  ChartDrawLoading as ChartSkeleton,
  type ChartDrawLoadingProps as ChartSkeletonProps,
} from "./ChartDrawLoading";

// Uniwind augments react-native's component props with `className`.
// Referencing this here so packages/ui can typecheck without depending on apps/mobile.
/// <reference types="uniwind/types" />
export {};

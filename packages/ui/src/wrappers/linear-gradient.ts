/**
 * expo-linear-gradient — thin re-export (ADR 006 §决策二).
 * Required by HeroUI Native `ScrollShadow` (`LinearGradientComponent` prop).
 */

export { LinearGradient } from "expo-linear-gradient";
export type { LinearGradientProps } from "expo-linear-gradient";
